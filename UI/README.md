# UI

Vanilla JavaScript Progressive Web App shell.

- `index.html` entry page
- `app.js` client bootstrap and install prompt handling
- `service-worker.js` offline cache
- `manifest.webmanifest` PWA manifest
